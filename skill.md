---
name: roommate-utility-calculator
description: 協助計算透天厝分租套房的複雜水電分帳（含按天數、人數、基本費拆分邏輯），並生成室友通知訊息。
version: 1.0.0
triggers:
  - 收到新一期水電帳單
  - 到期抄表（如 8/19）需要計算分帳
---

# 🏠 室友水電分帳計算技能指南

當使用者要求計算水電費、更新電表度數或初始化水電 APP 專案時，請嚴格遵循以下邏輯與開發規範。

## 1. 基礎資料與初始設定 (Data Schema)

本專案核心包含六間房間，其初始狀態與入住資訊如下，請將其建立為資料庫或設定檔（如 `rooms_config.json`）：

| 房號 | 室友名稱 | 初始電表度數 (當前) | 真正入住日期 | 入住人數 |
| :--- | :--- | :--- | :--- | :--- |
| **3A** | 吳小姐 (我) | 493 | 2026-07-09 (四) | 1 |
| **3B** | 林小姐 | 1069 | 2026-06-18 (四) | 1 |
| **4C** | 柯先生 | 4582 | 2026-05-29 (五) | 1 |
| **4D** | 姚小姐/金先生 | 541 | 2026-07-04 (土) | 2 |
| **5E** | 甘小姐/王先生 | 4180 | 2026-05-25 (一) | 2 |
| **6F** | 劉小姐 | 3575 | 2026-05-29 (五) | 1 |

*   **關鍵日程提醒**：下次收到帳單與抄表日為 **2026-08-19（三）**。

## 2. 核心計算邏輯 (Calculation Logic)

### 💡 電費計算
1. **各房用電量** = `8/19 新抄表度數` - `上述初始電表度數`。
2. **總電費分攤** = 依據帳單計算出每度電單價（或依房東規定固定單價），乘以各房用電量。

### 💧 水費計算（葫洲美好際寓透天厝特殊邏輯）
整棟共用一個門牌與總水表，帳單拆分為兩部分計算：

1. **基本費分攤**：
   * 先將基本費平均分為 6 等分（A～F 六間房）。
   * 每間房再依據「該期帳單計費區間內，**實際入住天數**」佔滿額天數的比例分攤。
   * *註：若有未出租/未入住期間的基本費，由房東負擔，不轉嫁給其他室友。*
2. **用水費及水源保育費分攤**：
   * 權重權重計算 = `各房入住人數` × `各房該期實際入住天數`。
   * 依據各房的「人數×天數權重」比例，分攤總用水費與水源保育費。

## 3. APP 功能需求與開發步驟

請協助使用者使用 Python / Web 技術（如 Flask/FastAPI + 前端，或 Streamlit 快速原型）開發此 APP：

### 階段一：後端核心邏輯與測試 (`src/calculator.py`)
* 實作日期天數計算功能（需能計算入住日至 8/19 的實際天數）。
* 實作上述水費與電費的拆分演算法。
* 撰寫測試案例，確保當輸入「總水費、總電費、8/19 各房新度數」時，能精準算出每房應繳金額。

### 階段二：前端介面開發
* 提供簡單直覺的表單：
  1. 輸入框：台電總電費、台水總基本費、台水總用水費（含保育費）。
  2. 欄位：輸入 6 間房在 8/19 抄到的新電表度數。
* 顯示結果：清爽的表格，列出每間房的「天數、人數、電費、水費基本費、實際水費、總計」。

### 階段三：自動生成 Line 群組通知文字
* APP 需有一鍵複製功能，生成格式如下的通知文字：
  > 📢 **【美好際寓 水電費通知】**
  > 計算區間：[帳單起始日] ~ 8/19
  > 
  > 🔹 **3A 吳小姐**：電費 $X + 水費 $Y = 總計 $Z
  > 🔹 **4D 姚小姐/金先生**：電費 $X + 水費 $Y = 總計 $Z
  > ...
  > 匯款帳號：XXXX-XXXX-XXXX
  > 請於 X/X 前匯款，感謝大家配合！

## 4. 驗收標準 (Definition of Done)
* 驗證 4D 與 5E 的水費權重（人數為 2）有正確加權。
* 驗證各房天數計算正確（例如 3A 從 7/9 住到 8/19 共 41 天）。
* 算出來的室友應繳總額 + 房東應吸收基本費 = 帳單總額。